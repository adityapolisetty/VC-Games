[theme]
# Force light mode regardless of system setting
base = "light"

# Optionally keep colors aligned with the app's styling
backgroundColor = "#ffffff"
secondaryBackgroundColor = "#ffffff"
textColor = "#000000"
